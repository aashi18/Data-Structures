#define DEFAULT_SIZE 10
class s 
{
  
     
  public:
     int size;
     int top;
     int* values;
     Stack(int size = DEFAULT_SIZE);
     virtual ~Stack();
     bool isFull();
     bool isEmpty();
     void push(int);
     int pop();
};
